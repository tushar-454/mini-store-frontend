import fail from '../assets/fail.webp';

const Fail = () => {
  return (
    <div className='flex items-center justify-center'>
      <img data-aos='zoom in' src={fail} alt='Fail image' />
    </div>
  );
};

export default Fail;
