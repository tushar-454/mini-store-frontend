import cancel from '../assets/cancel.webp';

const Cancel = () => {
  return (
    <div className='flex items-center justify-center'>
      <img data-aos='zoom in' src={cancel} alt='cancel image' />
    </div>
  );
};

export default Cancel;
